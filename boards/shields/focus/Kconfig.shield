config SHIELD_focus
    def_bool $(shields_list_contains, focus)

# enabled the NFC pins
config NFCT_PINS_AS_GPIOS
	default y

config TRACING
    default n

config DEBUG
    default n

config BUILD_OUTPUT_UF2
	default n

CONFIG_SERIAL=n